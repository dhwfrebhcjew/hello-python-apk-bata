name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 60  # 增加到60分钟，首次下载SDK可能较慢

    steps:
    - name: 1. 下载代码
      uses: actions/checkout@v4

    - name: 2. 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: 3. 安装系统依赖
      run: |
        sudo apt-get update -y
        sudo apt-get install -y \
          python3-pip \
          python3-dev \
          build-essential \
          git \
          wget \
          zip \
          unzip \
          openjdk-17-jdk \
          zlib1g-dev \
          libffi-dev \
          libssl-dev \
          libltdl-dev \
          pkg-config \
          autoconf \
          automake \
          libtool

    - name: 4. 安装 Python 构建工具
      run: |
        python -m pip install --upgrade pip setuptools wheel
        pip install --no-cache-dir \
          buildozer \
          cython==0.29.33 \
          virtualenv

    - name: 5. 显示环境信息
      run: |
        echo "=== 环境信息 ==="
        echo "系统版本: $(lsb_release -ds)"
        echo "Python版本: $(python --version)"
        echo "当前目录: $(pwd)"
        echo "目录内容:"
        ls -la

    - name: 6. 检查并修复 buildozer.spec 配置
      run: |
        echo "检查 buildozer.spec 配置..."
        if [ -f "buildozer.spec" ]; then
          echo "✅ 找到 buildozer.spec 文件"
          if grep -q "version.regex" buildozer.spec; then
            echo "修复: 移除 version.regex 配置"
            sed -i '/version\.regex/d' buildozer.spec
          fi
        else
          echo "❌ 未找到 buildozer.spec，创建默认配置"
          cat > buildozer.spec << 'EOF'
[app]
title = Hello Python APK
package.name = hellopythonapk
package.domain = com.example
source.dir = .
source.include_exts = py,png,jpg,kv,atlas,txt
version = 1.0.0
requirements = python3,kivy==2.1.0
orientation = portrait
fullscreen = 0
android.minapi = 21
android.sdk = 30
android.permissions = INTERNET
android.arch = arm64-v8a

[buildozer]
log_level = 2
EOF
        fi

    - name: 7. 设置 Android SDK 许可接受（关键修复）
      run: |
        echo "设置 Android SDK 许可自动接受..."
        # 创建 Android SDK 目录
        mkdir -p ~/.android
        
        # 创建 licenses 目录
        mkdir -p ~/.android/licenses
        
        # 预先写入所有需要的许可协议接受文件
        # 这是 Android SDK 要求的自动接受方式
        echo "8933bad161af4178b1185d1a37fbf41ea5269c55" > ~/.android/licenses/android-sdk-license
        echo "84831b9409646a918e30573bab4c9c91346d8abd" > ~/.android/licenses/android-sdk-preview-license
        echo "d975f751698a77b662f1254ddbeed3901e976f5a" > ~/.android/licenses/intel-android-extra-license
        
        echo "✅ Android SDK 许可已自动接受"
        
        # 设置环境变量，确保在构建过程中也接受许可
        echo "ANDROID_SDK_ROOT=$HOME/.buildozer/android/platform/android-sdk" >> $GITHUB_ENV
        echo "ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk" >> $GITHUB_ENV
        echo "YES_I_HAVE_THE_RIGHT_TO_USE_THESE_PACKAGES=yes" >> $GITHUB_ENV

    - name: 8. 执行 APK 构建（修复许可问题）
      id: build-apk
      continue-on-error: false
      run: |
        echo "=== 开始执行 APK 构建 ==="
        
        # 设置构建超时并记录日志
        # 首次构建可能需要更长时间来下载 Android SDK
        timeout 3600 bash -c '
          echo "Buildozer 构建开始: $(date)"
          
          # 强制接受所有 Android 许可
          echo "正在接受 Android SDK 许可..."
          yes | $HOME/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager --licenses 2>&1 || true
          
          echo "开始执行 buildozer..."
          buildozer android debug 2>&1
          BUILD_EXIT=${PIPESTATUS[0]}
          echo "Buildozer 构建结束，退出码: $BUILD_EXIT"
          exit $BUILD_EXIT
        ' 2>&1 | tee build_output.log
        
        BUILD_STATUS=$?
        echo "=== 整体构建过程退出代码: $BUILD_STATUS ==="
        
        # 检查 APK 是否生成
        if ls bin/*.apk 1> /dev/null 2>&1; then
          echo "✅ 构建成功: 找到 APK 文件！"
          APK_FILE=$(ls bin/*.apk | head -1)
          echo "APK 文件: $APK_FILE"
          echo "文件大小: $(du -h "$APK_FILE" | cut -f1)"
          echo "::notice title=构建成功::APK 已生成: $APK_FILE"
          
          # 显示成功信息
          echo ""
          echo "🎉 恭喜！APK 构建成功！"
          echo "📱 您可以在上方的 'Artifacts' 区域下载生成的 APK 文件"
          echo "🔧 文件名为: android-apk"
        else
          echo "❌ 构建失败: 未找到 APK 文件"
          echo "=== 构建日志最后 50 行 ==="
          tail -50 build_output.log
          echo "=== 日志结束 ==="
          
          if [ $BUILD_STATUS -eq 124 ]; then
            echo "错误类型: 构建超时 (超过60分钟)"
          elif [ $BUILD_STATUS -ne 0 ]; then
            echo "错误类型: 构建过程错误 (退出码: $BUILD_STATUS)"
          fi
          
          echo "::error title=构建失败::APK 构建失败，请检查日志"
          exit 1
        fi

    - name: 9. 上传构建日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs
        path: build_output.log
        if-no-files-found: warn
        retention-days: 14

    - name: 10. 上传生成的 APK 文件
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: bin/*.apk
        if-no-files-found: error
        retention-days: 30

    - name: 11. 构建失败诊断
      if: failure()
      run: |
        echo "=== 构建失败诊断 ==="
        echo "诊断时间: $(date)"
        
        echo "1. 检查关键文件:"
        for file in "buildozer.spec" "main.py"; do
          if [ -f "$file" ]; then
            echo "   ✅ $file 存在"
          else
            echo "   ❌ $file 不存在"
          fi
        done
        
        echo ""
        echo "2. 检查 Android SDK 许可:"
        if [ -d "$HOME/.android/licenses" ]; then
          echo "   ✅ Android 许可目录存在"
          echo "   许可文件:"
          ls -la $HOME/.android/licenses/
        else
          echo "   ❌ Android 许可目录不存在"
        fi
